generator client { provider = "prisma-client-js" }
datasource db { provider = "postgresql" url = env("DATABASE_URL") }
model ProductLead { id String @id @default(cuid()) title String url String? score Int? createdAt DateTime @default(now()) }
